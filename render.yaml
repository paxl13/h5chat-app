services:
  - type: web
    name: h5chat-app
    runtime: node
    buildCommand: npm install && npm run build:prod
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production